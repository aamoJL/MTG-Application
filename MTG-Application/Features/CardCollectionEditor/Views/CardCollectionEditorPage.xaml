<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="MTGApplication.Features.CardCollectionEditor.Views.CardCollectionEditorPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:controls="using:CommunityToolkit.WinUI.Controls" 
  xmlns:helpers="using:MTGApplication.General.Views.BindingHelpers" 
  xmlns:sys="using:System"
  xmlns:customControls="using:MTGApplication.General.Views.Controls"
  xmlns:ui="using:CommunityToolkit.WinUI"
  xmlns:models="using:MTGApplication.Features.CardCollectionEditor.Models"
  xmlns:interactivity="using:Microsoft.Xaml.Interactivity" 
  xmlns:converters1="using:MTGApplication.General.Views.BindingHelpers.Converters" 
  xmlns:views="using:MTGApplication.Features.CardCollectionEditor.Views" 
  xmlns:controls1="using:MTGApplication.Features.CardCollectionEditor.Views.Controls" 
  xmlns:editorpage="using:MTGApplication.Features.CardCollectionEditor.ViewModels.EditorPage" 
  xmlns:converters="using:MTGApplication.Features.CardCollectionEditor.Views.Controls.Converters" 
  xmlns:collectionlist="using:MTGApplication.Features.CardCollectionEditor.ViewModels.CollectionList" 
  xmlns:collectioncard="using:MTGApplication.Features.CardCollectionEditor.ViewModels.CollectionCard"
  mc:Ignorable="d"
  d:DataContext="{d:DesignInstance Type=editorpage:CardCollectionEditorPageViewModel}"
  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
  KeyboardAcceleratorPlacementMode="Hidden">

  <Page.Resources>
    <converters:BoolToSelectionModeConverter x:Key="BoolToSelectionModeConverter" />
    <converters1:NullToBooleanConverter Invert="True" x:Key="NotNullToBooleanConverter"/>
  </Page.Resources>

  <!--#region Keyboard Accelerators -->
  <!--NOTE: KeyboardAccelerators needs to be here because menu flyout accelerators does not work if the flyout has not been opened-->
  <Page.KeyboardAccelerators>
    <KeyboardAccelerator Modifiers="Control" Key="N" Invoked="NewCollectionKeyboardAccelerator_Invoked" />
    <KeyboardAccelerator Modifiers="Control" Key="S" Invoked="SaveCollectionKeyboardAccelerator_Invoked" />
    <KeyboardAccelerator Modifiers="Control" Key="O" Invoked="OpenCollectionKeyboardAccelerator_Invoked" />
  </Page.KeyboardAccelerators>
  <!--#endregion-->

  <Grid>

    <!--#region Visual State Manager -->
    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup x:Name="CollectionNameTitleSaveState">
        <!--NOTE: Changes name text to "Unsaved" if the collection has no name-->
        <VisualState>
          <VisualState.StateTriggers>
            <ui:IsEqualStateTrigger Value="{x:Bind ViewModel.CollectionName, Mode=OneWay}" To="{x:Bind sys:String.Empty}" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="SelectedCollectionNameTitle.Text" Value="Unsaved" />
            <Setter Target="SelectedCollectionNameTitle.FontStyle" Value="Italic" />
            <Setter Target="SelectedCollectionNameTitle.Foreground" Value="LightGray" />
          </VisualState.Setters>
        </VisualState>

        <!--NOTE: Changes name text to different color to show that the collection has unsaved changes-->
        <VisualState>
          <VisualState.StateTriggers>
            <ui:IsEqualStateTrigger Value="{x:Bind ViewModel.CollectionViewModel.SaveStatus.HasUnsavedChanges, Mode=OneWay}" To="True" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="SelectedCollectionNameTitle.Foreground" Value="{ThemeResource SystemAccentColorLight1}" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
    <!--#endregion-->

    <controls:DockPanel LastChildFill="True" Padding="0 0 0 10">

      <!--#region Command bar -->
      <Grid controls:DockPanel.Dock="Top" BorderThickness="0 0 0 1" BorderBrush="{ThemeResource RegionColor}">
        <StackPanel Orientation="Horizontal">

          <!--#region File: Collection -->
          <AppBarButton Label="Collection">
            <AppBarButton.Icon>
              <FontIcon Glyph="&#xE8E5;" />
            </AppBarButton.Icon>
            <AppBarButton.Flyout>
              <MenuFlyout AreOpenCloseAnimationsEnabled="False" Placement="Bottom">
                <MenuFlyoutItem Text="New" Command="{x:Bind ViewModel.NewCollectionCommand, Mode=OneTime}" KeyboardAcceleratorTextOverride="Ctrl+N" />
                <MenuFlyoutItem Text="Open..." Command="{x:Bind ViewModel.OpenCollectionCommand, Mode=OneTime}" KeyboardAcceleratorTextOverride="Ctrl+O" />
                <MenuFlyoutItem Text="Save..." Command="{x:Bind ViewModel.CollectionViewModel.SaveCollectionCommand, Mode=OneWay}" KeyboardAcceleratorTextOverride="Ctrl+S" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Delete Collection" Command="{x:Bind ViewModel.CollectionViewModel.DeleteCollectionCommand, Mode=OneWay}" Background="IndianRed" />
              </MenuFlyout>
            </AppBarButton.Flyout>
          </AppBarButton>
          <!--#endregion-->

          <AppBarSeparator />

          <!--#region File: List -->
          <AppBarButton Label="List">
            <AppBarButton.Icon>
              <FontIcon Glyph="&#xE8E5;" />
            </AppBarButton.Icon>
            <AppBarButton.Flyout>
              <MenuFlyout AreOpenCloseAnimationsEnabled="False" Placement="Bottom">
                <MenuFlyoutItem Text="New..." Command="{x:Bind ViewModel.CollectionViewModel.AddNewListCommand, Mode=OneWay}" />
                <MenuFlyoutItem Text="Edit..." 
                                Command="{x:Bind ViewModel.CollectionViewModel.ListViewModel.EditListCommand, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.CollectionViewModel.ListViewModel, Converter={StaticResource NotNullToBooleanConverter}, Mode=OneWay}"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Import..." 
                                Command="{x:Bind ViewModel.CollectionViewModel.ListViewModel.ImportCardsCommand, Mode=OneWay}" 
                                IsEnabled="{x:Bind ViewModel.CollectionViewModel.ListViewModel, Converter={StaticResource NotNullToBooleanConverter}, Mode=OneWay}"/>
                <MenuFlyoutItem Text="Export..." 
                                Command="{x:Bind ViewModel.CollectionViewModel.ListViewModel.ExportCardsCommand, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.CollectionViewModel.ListViewModel, Converter={StaticResource NotNullToBooleanConverter}, Mode=OneWay}"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Delete List" 
                                Command="{x:Bind ViewModel.CollectionViewModel.ListViewModel.DeleteListCommand, Mode=OneWay}" 
                                IsEnabled="{x:Bind ViewModel.CollectionViewModel.ListViewModel, Converter={StaticResource NotNullToBooleanConverter}, Mode=OneWay}"
                                Background="IndianRed" />
              </MenuFlyout>
            </AppBarButton.Flyout>
          </AppBarButton>
          <!--#endregion-->

          <!--#region List Selector-->
          <customControls:ComboBoxWithCommands
            ItemsSource="{x:Bind ViewModel.CollectionViewModel.CollectionListViewModels, FallbackValue={x:Null}, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.CollectionViewModel.ListViewModel, FallbackValue={x:Null}, Mode=OneWay}"
            SelectionChangedCommand="{x:Bind ViewModel.CollectionViewModel.ChangeListCommand, Mode=OneWay}"
            VerticalAlignment="Center"
            Width="200"
            Margin="5 0 5 0"
            IsEnabled="{x:Bind helpers:Comparison.MoreThan(ViewModel.CollectionViewModel.CollectionListViewModels.Count, 0), Mode=OneWay}">
            <ComboBox.ItemTemplate>
              <DataTemplate x:DataType="collectionlist:CardCollectionListViewModel">
                <TextBlock Text="{Binding Name, Mode=OneWay}" />
              </DataTemplate>
            </ComboBox.ItemTemplate>
          </customControls:ComboBoxWithCommands>
          <!--#endregion-->

          <AppBarSeparator />

          <!--#region Collection Info -->
          <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="10 0 0 0">
            <TextBlock x:Name="SelectedCollectionNameTitle" Text="{x:Bind ViewModel.CollectionName, Mode=OneWay}" FontWeight="Bold" />
            <TextBlock>
              <Run Text="{x:Bind ViewModel.CollectionViewModel.ListViewModel.Cards.Count, FallbackValue='0', Mode=OneWay}" />
              <Run Text="/"/>
              <Run Text="{x:Bind ViewModel.CollectionViewModel.ListViewModel.QueryCards.TotalCardCount, FallbackValue='0', Mode=OneWay}" />
              <Run Text="cards collected"/>
            </TextBlock>
          </StackPanel>
          <!--#endregion-->

          <ProgressRing IsActive="{x:Bind ViewModel.Worker.IsBusy, Mode=OneWay}" Margin="10 0 0 0" />
        </StackPanel>

        <!--#region Card View Information -->
        <Grid HorizontalAlignment="Right" Margin="0 5 5 0">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Spacing="5">
            <ToggleButton x:Name="SingleTapSelectionModeSwitch" ToolTipService.ToolTip="Single tap">
              <FontIcon Glyph="&#xE815;" />
            </ToggleButton>
            <Button Command="{x:Bind SwitchWindowThemeCommand, Mode=OneTime}" ToolTipService.ToolTip="Switch theme">
              <FontIcon Glyph="&#xE793;"/>
            </Button>
          </StackPanel>

          <TextBlock VerticalAlignment="Bottom">
            <Run Text="{x:Bind ViewModel.CollectionViewModel.ListViewModel.QueryCards.Collection.Count, FallbackValue='0', Mode=OneWay}" />
            <Run Text="/"/>
            <Run Text="{x:Bind ViewModel.CollectionViewModel.ListViewModel.QueryCards.TotalCardCount, FallbackValue='0', Mode=OneWay}" />
            <Run Text="cards loaded"/>
          </TextBlock>
        </Grid>
        <!--#endregion-->

      </Grid>
      <!--#endregion-->

      <!--#region Card View -->
      <ScrollViewer 
        VerticalScrollBarVisibility="Auto"
        controls:DockPanel.Dock="Top">
        <ItemsRepeater
          ItemsSource="{x:Bind ViewModel.CollectionViewModel.ListViewModel.QueryCards.Collection, FallbackValue={x:Null}, Mode=OneWay}"
          Style="{StaticResource GridItemsRepeaterStyle}">
          <ItemsRepeater.Layout>
            <UniformGridLayout MinItemWidth="230" ItemsStretch="Fill"/>
          </ItemsRepeater.Layout>
          <interactivity:Interaction.Behaviors>
            <helpers:ItemsRepeaterIncrementalLoading 
              LoadingOffset="200"
              IsLoadingMore="{x:Bind ViewModel.CollectionViewModel.ListViewModel.QueryCards.Collection.IsLoading, Mode=OneWay}"/>
          </interactivity:Interaction.Behaviors>
          <ItemsRepeater.ItemTemplate>
            <DataTemplate x:DataType="collectioncard:CardCollectionMTGCardViewModel">
              <controls1:CardCollectionCardImageView
                Model="{x:Bind Mode=OneTime}"
                ShowPrintsCommand="{x:Bind ShowPrintsCommand, Mode=OneTime}"
                SwitchOwnershipCommand="{x:Bind SwitchOwnershipCommand, Mode=OneTime}"
                OpenAPIWebsiteCommand="{x:Bind OpenAPIWebsiteCommand, Mode=OneTime}"
                OpenCardmarketWebsiteCommand="{x:Bind OpenCardMarketWebsiteCommand, Mode=OneTime}"
                SelectionMode="{Binding ElementName=SingleTapSelectionModeSwitch, Path=IsChecked, Converter={StaticResource BoolToSelectionModeConverter}}"/>
            </DataTemplate>
          </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
      </ScrollViewer>
      <!--#endregion-->

    </controls:DockPanel>
  </Grid>
</Page>